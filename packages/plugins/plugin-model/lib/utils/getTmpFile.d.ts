import { ModelItem } from '.';
export declare const getTmpFile: (files: string[], extra: ModelItem[] | undefined, absSrcPath: string) => {
    providerContent: string;
    useModelContent: string;
};
