{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "lerna run dev --parallel",
    "build": "lerna bootstrap && yarn run clean && lerna run build",
    "clean": "rm -rf packages/**/lib"
  },
  "devDependencies": {
    "@chydan/eslint-config-samoa": "^1.2.4",
    "@types/node": "^17.0.40",
    "eslint": "^8.17.0",
    "lerna": "^5.0.0",
    "prettier": "^2.6.2"
  }
}
